# Proxy Quality Scanner Final-Fix Checklist

**Purpose:** executable completion checklist compiled from the design, implementation plan, and final review.  It is deliberately a read-only-audit handoff: no product change is implied by a checked item until its test/evidence is attached.

**Audit comparison note:** no `proxy_evidence_audit` file or text is present anywhere in this workspace (`rg --hidden` performed 2026-07-21).  Therefore items marked **Review-only candidate** are final-review findings that should be explicitly compared with the earlier summary before closing the work; they are not asserted to be absent from a summary that was not available to this audit.

## Baseline contract and non-negotiable constraints

- [ ] Proxy credentials enter only through `CLOAK_TEST_PROXY`, never a CLI proxy argument; no credentials, auth headers, cookies, CAPTCHA tokens, or browser storage are logged/serialized.  **Contract:** [design](../../docs/superpowers/specs/2026-07-21-proxy-quality-scanner-design.md) lines 11-23, 121-132; [plan](../../docs/superpowers/plans/2026-07-21-proxy-quality-scanner.md) lines 11-20.  **Current anchors:** `benchmarks/proxy_quality.py:89-120, 312-325, 334-380`; `benchmarks/proxy_quality_models.py:194-247`.  **Tests:** extend `tests/test_proxy_quality_models.py:61-84` and `tests/test_proxy_quality_cli.py:323-368`.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_models.py tests/test_proxy_quality_cli.py -q`.
- [ ] Keep deterministic model/aggregation code independent of network adapters; live checks remain opt-in, send at most one initial request to each protected site, and never solve/click CAPTCHA.  **Contract:** plan lines 5-9, 16-20, 388-401; design lines 84-92, 143-155.  **Current anchors:** `benchmarks/proxy_site_checks.py:78-117, 120-194`; `benchmarks/proxy_quality.py:262-327`.  **Tests:** `tests/test_proxy_site_checks.py`, `tests/test_proxy_quality_cli.py`; add call-count assertions.  **Deterministic command:** `python -m pytest tests/test_proxy_site_checks.py tests/test_proxy_quality_cli.py -q`.
- [ ] Preserve the independent report sections and the specified summary contract: `connectivity`, `classification`, `reputation_intelligence`, `identity_alignment`, `site_outcomes`, `summary`; summary includes `type`, `type_confidence`, `reputation`, and `suitable_for_protected_sites` (`yes`/`no`/`uncertain`).  **Contract:** design lines 94-119.  **Current anchors:** `benchmarks/proxy_quality.py:306-316`; `benchmarks/proxy_quality_models.py:154-191`.  **Tests:** revise exact-key assertions in `tests/test_proxy_quality_cli.py:95-105`.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_models.py tests/test_proxy_quality_cli.py -q`.

## Critical blockers

### C1. Credential-safe parsing, validation, redaction, and runtime launch

- [ ] Replace first-`@` URI redaction with parsing/reconstruction from validated scheme, host, and port.  Correctly redact raw `@`, percent-encoded `@`, empty user/password cases, IPv6, colon proxy format, and malformed input; never return an unvalidated credential-bearing string.  **Final review:** lines 11-17.  **Current code:** `benchmarks/proxy_quality_models.py:52-55, 194-209`; CloakBrowser behavior to remain compatible: `tests/test_proxy.py:316-320`.  **Tests to add:** special-character and IPv6 cases in `tests/test_proxy_quality_models.py` (current only simple case at lines 82-84); guard regression through `_build_secret_set()` in `tests/test_proxy_quality_cli.py`.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_models.py tests/test_proxy_quality_cli.py tests/test_proxy.py -q`.
- [ ] Reject path, query, fragment, malformed/ambiguous authority, missing port where required by proxy convention, and unparseable credentials before any output or network/browser use.  Errors must omit the supplied URL.  **Final review:** lines 13-17.  **Current code:** `benchmarks/proxy_intelligence.py:96-115`; `benchmarks/proxy_quality_models.py:194-209`; `benchmarks/proxy_quality.py:89-120`.  **Tests to add:** validation matrix including uncredentialed `socks5://host:1080/path?secret#secret` and raw/encoded special characters in `tests/test_proxy_intelligence.py`; CLI assertion that invalid input is not echoed in `tests/test_proxy_quality_cli.py`.  **Deterministic command:** `python -m pytest tests/test_proxy_intelligence.py tests/test_proxy_quality_cli.py -q`.
- [ ] Eliminate raw credential exposure through Chromium's `--proxy-server=` argument.  Browser-enabled scanning needs a credential-free launch mechanism (for example, a local authenticated relay or a secure core proxy-auth route); no raw proxy may reach `launch_persistent_context()`.  **Final review:** lines 13-17.  **Current flow:** `benchmarks/proxy_site_checks.py:93-101`; process-argument construction: `cloakbrowser/browser.py:1523-1568`.  **Tests to add:** unit seam asserting `launch_persistent_context` receives only a credential-free local endpoint; local authenticated relay integration fixture; process-argument inspection that excludes username/password/raw proxy.  **Deterministic command:** `python -m pytest tests/test_proxy_site_checks.py tests/test_proxy_quality_cli.py tests/test_launch.py -q`; then run the local fixture without internet.
- [ ] Expand the secret guard to scan every serialized/printed artifact plus PNG metadata, and ensure exception text is scrubbed.  **Final review:** lines 86-89, 104-106; design lines 123-132, 168-175.  **Current code:** `benchmarks/proxy_quality.py:214-221, 224-259, 318-326`; `benchmarks/proxy_site_checks.py:281-289`.  **Tests to add:** crafted PNG metadata containing a credential/token, staged-write abort before `Path.replace`, and error-path tests.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_models.py tests/test_proxy_quality_cli.py tests/test_proxy_site_checks.py -q`.

### C2. Real, tri-state identity alignment and clean-verdict gating

- [ ] Implement the required dedicated browser identity/leak stage: source-attributed HTTP exit IP, WebRTC-visible IP, timezone, locale, and DNS behavior.  Compare each independently and retain only safe observations/mismatch states.  **Final review:** lines 19-28.  **Design:** lines 73-82.  **Current gap:** `benchmarks/proxy_site_checks.py:78-117` only checks two sites; `benchmarks/proxy_quality.py:183-200` derives `aligned` from enrichment signals.  **Tests to add:** deterministic parser/normalizer fixtures for each signal and local browser/HTTP harness; no real token, cookie, or storage data.  **Deterministic command:** `python -m pytest tests/test_proxy_site_checks.py tests/test_proxy_quality_cli.py tests/test_proxy_quality_models.py -q`.
- [ ] Make alignment tri-state (`aligned` / `mismatch` / `unavailable` or equivalent explicit evidence status).  Empty observations must be unavailable, not aligned; an actual mismatch must be independently reported and make reputation `questionable`.  `clean_observed` requires complete successful identity evidence.  **Final review:** lines 21-28.  **Current false positive:** `benchmarks/proxy_quality.py:183-200`; accepted by `benchmarks/proxy_quality_models.py:162-185`; live proof `benchmarks/results/proxy-quality/20260721T110600.808211Z/report.json:17-20`.  **Tests to replace:** `tests/test_proxy_quality_models.py:61-69`, `tests/test_proxy_quality_cli.py:182-204`.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_models.py tests/test_proxy_quality_cli.py -q`.
- [ ] Preserve a fixed test seed, `fingerprint_preset="consistent"`, `geoip=True`, dedicated temporary profile, and close-before-cleanup behavior while adding the leak stage.  **Contract:** design lines 75-82; plan lines 340-347.  **Current anchors:** `benchmarks/proxy_site_checks.py:93-116`; `benchmarks/proxy_quality.py:284-293`.  **Tests:** add launch argument/profile lifecycle assertions to `tests/test_proxy_site_checks.py`.  **Deterministic command:** `python -m pytest tests/test_proxy_site_checks.py tests/test_persistent_context.py -q`.

### C3. Intelligence availability and complete classification evidence

- [ ] Propagate each required source's availability/status into `reputation_intelligence` and summary aggregation.  An unavailable selected abuse source cannot be treated as an empty negative match and cannot yield `clean_observed`; it must yield `unknown` unless higher-priority evidence makes the result blocked/questionable.  **Final review:** lines 30-36.  **Contract:** design lines 25-36, 112-119.  **Current anchors:** `benchmarks/proxy_intelligence.py:463-506`; `benchmarks/proxy_quality.py:298-305`; `benchmarks/proxy_quality_models.py:162-185`.  **Tests to add:** all unavailable, one unavailable/others clean, unavailable plus high/other match, and cache manifest availability cases in `tests/test_proxy_intelligence.py` and `tests/test_proxy_quality_models.py`.  **Deterministic command:** `python -m pytest tests/test_proxy_intelligence.py tests/test_proxy_quality_models.py tests/test_proxy_quality_cli.py -q`.
- [ ] Add `residential_or_isp` to `NetworkType`, classify structured ISP/residential evidence, and retain ASN/organization/category/carrier/mobile/hosting/privacy evidence in a sanitized report.  Local ASN-only evidence should be `residential_or_isp` where appropriate but must not force AS3257/GTT to residential; explain `unknown` with retained evidence if no reliable class exists.  **Final review:** lines 38-45.  **Design:** lines 47-64.  **Current gap:** `benchmarks/proxy_quality_models.py:15-21, 93-147`; evidence dropped between `benchmarks/proxy_intelligence.py:614-630` and `benchmarks/proxy_quality.py:280-315`; current exact-key test `tests/test_proxy_quality_cli.py:95-105`.  **Tests to add:** ISP/residential structured signals, AS3257 unknown-with-evidence, serialization/redaction.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_models.py tests/test_proxy_intelligence.py tests/test_proxy_quality_cli.py -q`.
- [ ] Require conflict confidence to be `low` regardless of a source-count majority; preserve conflicts and distinguish independent structured sources from ASN-name heuristics.  Validate and type-normalize evidence fields so empty containers/arbitrary objects cannot count as mobile evidence.  **Final review:** lines 49-51, 84-87.  **Design:** lines 56-64.  **Current anchors:** `benchmarks/proxy_quality_models.py:58-72, 122-145`; current wrong test expectation `tests/test_proxy_quality_models.py:40-51`.  **Tests to add:** mobile-vs-hosting conflict with 2:1 votes, `carrier={}`, `mcc=[]`, arbitrary object, Telecom-only organization, and single/multiple independent source rules.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_models.py -q`.

## Important correctness and contract fixes

- [ ] Summary must include classification `type` and `type_confidence` and emit `suitable_for_protected_sites: "uncertain"`, not `"unknown"`, for unsupported conclusions.  Ensure timestamped/site-specific qualification accompanies `clean_observed`.  **Final review:** lines 53-55.  **Design:** lines 105-119.  **Current code:** `benchmarks/proxy_quality_models.py:40-45, 154-191`; report creation `benchmarks/proxy_quality.py:306-316`; docs `README.md:1383-1386`, `docs/CODEBASE_FUNCTIONALITY.md:370-373`.  **Tests to replace:** `tests/test_proxy_quality_cli.py:127-179`.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_models.py tests/test_proxy_quality_cli.py -q`.
- [ ] Implement safe `--output-dir <path>` parsing exactly as documented; continue rejecting all proxy/credential CLI arguments and invalid/missing option values with exit 2.  Use the selected root for timestamped report, source manifest, screenshots, and cache.  **Final review:** lines 57-59.  **Contract:** design lines 11-23; plan lines 464-472.  **Current code:** `benchmarks/proxy_quality.py:32, 334-357`; current contrary test `tests/test_proxy_quality_cli.py:376-383`.  **Tests to add/replace:** defaults, custom relative/absolute paths, malformed option, proxy-like unknown option; assert no raw proxy in stderr.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_cli.py -q`.
- [ ] Record connectivity evidence from both independent echo services: endpoint identity, three-request outcomes, median latency, exit-IP agreement, and country consistency (or explicit unavailable).  Require at least one success from each endpoint, not merely two successes from one endpoint.  **Final review:** lines 61-63.  **Design:** lines 40-45; plan lines 188-205.  **Current code:** `benchmarks/proxy_intelligence.py:22-25, 136-185`; serialized fields `benchmarks/proxy_quality.py:123-138`.  **Tests to add:** two same-service successes rejected, one from each accepted, median calculation, endpoint failure/status, country mismatch/unavailable.  **Deterministic command:** `python -m pytest tests/test_proxy_intelligence.py tests/test_proxy_quality_cli.py -q`.
- [ ] Enrich reputation matches with `source`, conservative list `category`, and `exact_ip` versus `cidr` `granularity`; retain selected-list policy and report absence only as `no_listed_abuse`, not proof of cleanliness.  **Final review:** lines 65-67.  **Design:** lines 66-71.  **Current code:** `benchmarks/proxy_intelligence.py:619-628`; restrictive serialization `benchmarks/proxy_quality.py:141-152`.  **Tests to add:** exact IPv4/IPv6, CIDR, IPsum/FireHOL categories, and safe report serialization in `tests/test_proxy_intelligence.py` and `tests/test_proxy_quality_cli.py`.  **Deterministic command:** `python -m pytest tests/test_proxy_intelligence.py tests/test_proxy_quality_cli.py -q`.
- [ ] Make a Cloudflare explicit-block verdict reachable using safe URL/status/body indicators and preserve parser precedence; do not interact with challenges.  **Final review:** lines 69-71.  **Current code:** `benchmarks/proxy_site_checks.py:61-75`; aggregation branch `benchmarks/proxy_quality_models.py:178-180`.  **Tests to add:** sanitized explicit block URL/status/body fixture in `tests/test_proxy_site_checks.py`, summary blocked test in `tests/test_proxy_quality_models.py`.  **Deterministic command:** `python -m pytest tests/test_proxy_site_checks.py tests/test_proxy_quality_models.py -q`.
- [ ] Capture Cloudflare/Google screenshots after detection even on timeout where technically possible; retain screenshot path/capture status in the report, use the specified top-level artifact names or document the intentional layout change, and strip/verify PNG metadata.  **Final review:** lines 73-77, 86-88.  **Design:** lines 123-130; plan lines 340-347.  **Current code:** `benchmarks/proxy_site_checks.py:120-151, 154-194, 281-289`; report discards fields in `benchmarks/proxy_quality.py:203-211, 283-315`; timeout test currently accepts omission at `tests/test_proxy_site_checks.py:134-169`.  **Deterministic command:** `python -m pytest tests/test_proxy_site_checks.py tests/test_proxy_quality_cli.py -q`.
- [ ] Add the Pixelscan report link and the explicit statement that the Turnstile endpoint is a third-party widget demo, not a universal Cloudflare reputation verdict.  **Final review:** lines 86-88.  **Contract:** design lines 84-92.  **Current report/docs anchors:** `benchmarks/proxy_quality.py:283-315`; `README.md:1350-1425`; `docs/CODEBASE_FUNCTIONALITY.md:345-391`.  **Tests:** assert safe static report metadata/link/qualification in `tests/test_proxy_quality_cli.py`.  **Deterministic command:** `python -m pytest tests/test_proxy_quality_cli.py -q`.
- [ ] Add local HTTP/SOCKS integration fixtures to exercise proxy transport configuration and cross-module evidence flow; keep them offline and free of real secrets.  **Final review:** line 89.  **Design:** lines 143-155.  **Target tests/fixtures:** `tests/test_proxy_intelligence.py`, `tests/test_proxy_quality_cli.py`, `tests/fixtures/proxy_quality/`.  **Deterministic command:** `python -m pytest tests/test_proxy_intelligence.py tests/test_proxy_quality_cli.py -q`.

## Documentation and ledger corrections

- [ ] Rewrite README no-browser wording: it skips protected browser checks but still performs proxy connectivity requests and may download/refresh intelligence data; it is not deterministic/offline.  Include full-confidence rules (two independent structured agreements = high; one nonconflicting structured source = medium; heuristic-only or conflict = low).  **Final review:** lines 80-82.  **Current docs:** `README.md:1368, 1390`; source behavior `benchmarks/proxy_quality.py:273-280`.  **Contract:** design lines 58-64, 159-166.  **Verification:** `rg -n "deterministic|offline|skip.browser|confidence" README.md docs/CODEBASE_FUNCTIONALITY.md` and deterministic docs/schema tests.
- [ ] Document installation (`python -m pip install -e ".[geoip]"` for SOCKS), environment-only credential input, full and no-browser commands with `--output-dir`, all summary fields/values, type-vs-reputation distinction, caveats (residential/mobile != clean; datacenter != automatically blocked), dataset licenses/attributions, cache behavior, request limits, unavailable source behavior, and `clean_observed` limitations.  **Plan:** lines 437-451; **design:** lines 157-166.  **Target docs:** `README.md:1350-1425`; `docs/CODEBASE_FUNCTIONALITY.md:345-391`.  **Verification:** manual command/doc parity plus `python -m pytest tests/test_proxy_quality_cli.py -q`.
- [ ] Update `docs/CODEBASE_FUNCTIONALITY.md` source map/scanner behavior so it agrees with the final public report schema and source availability semantics.  **Final review:** lines 36, 53-55; **current docs:** `docs/CODEBASE_FUNCTIONALITY.md:370-379`.  **Verification:** `rg -n "suitable_for_protected_sites|type_confidence|unavailable|clean_observed" README.md docs/CODEBASE_FUNCTIONALITY.md`.
- [ ] Update `.superpowers/sdd/progress.md` after Task 6 evidence is complete; current ledger ends at Task 5.  **Final review:** lines 73-78; **current file:** `.superpowers/sdd/progress.md:1-9`.  **Verification:** review that each final evidence artifact/test command has a dated outcome and no secret value.

## Acceptance evidence and requested verification (run only after fixes)

- [ ] **Compilation:** `python -m py_compile benchmarks/proxy_quality_models.py benchmarks/proxy_intelligence.py benchmarks/proxy_site_checks.py benchmarks/proxy_quality.py` must exit 0.  **Plan:** lines 453-462.
- [ ] **Complete deterministic suite (no internet):** `python -m pytest tests/test_proxy_quality_models.py tests/test_proxy_intelligence.py tests/test_proxy_site_checks.py tests/test_proxy_quality_cli.py tests/test_fingerprint_scanners.py -q`.  It must pass with mocked/local sources.  **Plan:** lines 409-422, 453-462; **design:** lines 143-155, 168-175.
- [ ] **Core regression suite:** `python -m pytest tests/test_fingerprint_preset.py tests/test_session.py tests/test_launch.py tests/test_persistent_context.py -q` must pass.  **Plan:** lines 478-486.
- [ ] **Browser request budget:** unit/local-fixture instrumentation proves no more than one initial navigation/request per protected site and no CAPTCHA action/retry.  **Design:** lines 84-92, 174-175; **plan:** lines 490-497.  **Deterministic command:** `python -m pytest tests/test_proxy_site_checks.py -q`.
- [ ] **Source cache/manifest:** offline test proves fresh cache reuse; expired refresh is atomic; failures preserve older valid cache; every enabled source including unavailable sources has URL/retrieval/checksum/status provenance.  **Plan:** lines 197-205, 232-281; **design:** lines 25-36, 123-130.  **Deterministic command:** `python -m pytest tests/test_proxy_intelligence.py -q`.
- [ ] **Current-proxy controlled live scan (explicitly opt-in):** with `CLOAK_TEST_PROXY` set only in the process environment, run `python -m benchmarks.proxy_quality --output-dir benchmarks/results/proxy-quality`.  It may return 0 with `blocked`; must reproduce the observed Google CAPTCHA without solving it, classify with retained evidence/confidence, and produce the timestamped report/manifest/screenshots.  **Plan:** lines 464-472; **design:** lines 168-175.  **Do not run until C1's credential-free browser path is implemented.**
- [ ] **Direct-network control (explicitly opt-in):** perform one separate, credential-free Google control that produces normal results and does not alter/provide evidence for the proxy report.  Save safe evidence.  **Final review:** lines 73-76; **design:** line 172.  **Required because the plan omitted it.**
- [ ] **Artifact privacy inspection (explicitly opt-in after live run):** search source/docs/tests/generated JSON/screenshots metadata for the actual username, password, raw proxy, IPinfo token, cookies, authorization header, token, HTML body, and browser storage; there must be no match.  Inspect `report.json`/`sources.json` and PNG metadata directly.  **Plan:** lines 474-476; **design:** lines 123-132, 168-175.  Use parameterized local secret fixtures for the deterministic counterpart; never place a real secret in shell history.
- [ ] **Re-run behavior (explicitly opt-in):** second scan must use cached intelligence and preserve the one-request-per-site budget.  Record cache timestamps/checksums and navigation counts.  **Design:** line 174.

## Review-only candidate findings to compare against the earlier evidence summary

The following are the final-review additions most likely to be absent from an artifact-only `proxy_evidence_audit`; do not close them merely because the existing report/screenshot contains no visible secret:

1. Runtime Chromium process-argument credential exposure (`final-review.md:13-17`; `benchmarks/proxy_site_checks.py:93-101`; `cloakbrowser/browser.py:1523-1568`).
2. First-`@` redaction leak and validation path/query/fragment leak (`final-review.md:13-17`; `benchmarks/proxy_quality_models.py:52-55,194-209`; `benchmarks/proxy_intelligence.py:96-115`).
3. Vacuous `identity_alignment.aligned: true` with no observations (`final-review.md:19-28`; `benchmarks/proxy_quality.py:183-200`; saved report `.../report.json:17-20`).
4. `clean_observed` despite unavailable intelligence (`final-review.md:30-36`; `benchmarks/proxy_intelligence.py:463-506`; `benchmarks/proxy_quality_models.py:162-185`).
5. Missing `residential_or_isp`, dropped ASN evidence, conflict-high confidence, and summary-schema incompatibility (`final-review.md:38-55`).
6. CLI/documentation mismatch for `--output-dir`, incomplete connectivity/reputation semantics, unreachable Cloudflare block, timeout screenshot loss, missing direct control, incorrect no-browser wording, missing screenshot metadata guard, and lack of integration fixture (`final-review.md:57-89`).

## Closure definition

- [ ] Every item above has a code/doc change, focused regression test, and passing deterministic command recorded in the ledger.
- [ ] Every live item has explicit opt-in evidence dated after the final fixes; no existing artifact substitutes for C1-C3.
- [ ] The report contract and README/functionality documentation agree byte-for-byte on field names/allowed values and caveats.
- [ ] A final read-only review finds no remaining Critical or Important item from `.superpowers/sdd/final-review.md:9-82`.
