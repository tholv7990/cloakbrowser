# Proxy quality scanner SDD progress

Plan: `docs/superpowers/plans/2026-07-21-proxy-quality-scanner.md`

Task 1: complete (9 tests passed; spec and quality reviews approved). Minor review note: malformed empty/non-scalar mobile fields could be filtered more strictly during final review.
Task 2: complete (17 tests passed; spec and quality reviews approved after transactional cache and credential-safety fixes).
Task 3: complete (25 tests passed; spec and quality reviews approved). Minor notes: select FireHOL level 2 by source name rather than manifest position; pin exact URL assertions during final review.
Task 4: complete (12 tests passed; spec and quality reviews approved after shared-deadline and widget-selector fixes).
Task 5: complete (18 focused and 69 aggregate tests passed; spec and quality reviews approved after artifact exit-code fixes). Minor report drift: an early paragraph still says 16 tests.
Task 6: complete. The separate direct-network control completed at `2026-07-21T11:46:13Z` with Google `results`, a captured/sanitized `google.png`, and `excluded_from_proxy_report: true` under `benchmarks/results/proxy-quality/direct-control/20260721T114613.017748Z/`. Three proxy runs then completed under `20260721T120143.213678Z/`, `20260721T120315.404689Z/`, and `20260721T122352.699808Z/`: all reproduced Google CAPTCHA, captured both protected-site screenshots, retained AS3257/GTT evidence as honest `unknown`/`low`, recorded SOCKS5 local-DNS identity mismatch, and summarized `blocked`/`no`. The second and third runs reused byte-identical source provenance (all three `sources.json` files have SHA-256 `15722f67cbe644bde4ea832d75eefb9c89d7aa3f3451a1c19f4afea4569f246e`; unchanged source retrieval times/checksums). Actual username/password scans across all three runs' `report.json` and `sources.json` returned no matches; structural inspection found no auth headers, cookies, CAPTCHA token values, HTML, profile paths, or browser storage, and all six proxy-run PNGs plus the direct-control PNG contained only `IHDR`/`IDAT`/`IEND` chunks.

Final correction pass (2026-07-21): ready for final re-review. The first two live runs exposed the checkbox/completeness defects: one Cloudflare screenshot showed success, the other an unchecked managed checkbox, and both reports retained `cloudflare: pending` plus the old `identity.complete: false`. The detector now distinguishes a visible in-frame checkbox (`interactive`) from a generic/loading widget (`pending`) and token-complete widget (`passed`), without interaction; identity `complete` now means all five dimensions were observed even if one mismatches. The third run, collected after those fixes, correctly recorded `identity.complete: true` but exposed a remaining token-probe defect: its screenshot visibly showed success while multiple response inputs caused strict `input_value()` lookup to fail and leave `cloudflare: pending`. A new RED regression (`1 failed`) uses two sanitized inputs with only the second populated and no success body text. The corrected in-page `evaluate_all()` predicate passes when any response input is nonempty and returns only a boolean, preserving success precedence without serializing token content. Focused GREEN is `4 passed`, the site suite is `28 passed`, the complete deterministic suite is `139 passed in 9.28s`, core launch/proxy regressions are `110 passed in 7.66s`, and all five scanner modules compile. README documents SOCKS5 local versus SOCKS5H remote DNS, and the design clarifies structured ISP mapping versus an uncategorized backbone ASN. No fourth live run was possible after the environment was cleared; all three prior reports remain unchanged for auditability. See `.superpowers/sdd/final-fix-report.md` and `.superpowers/sdd/final-fix-review.md`.

Superseding final acceptance: a fourth live run was subsequently completed at `20260721T123856.982677Z`. Current-parser output correctly reports Cloudflare `passed`, Google `captcha`, `identity.complete: true`, WebRTC aligned, expected SOCKS5 local-DNS mismatch, AS3257 evidence retained as `unknown` / `low`, and summary `blocked` / unsuitable `no`. Actual username/password scans of its JSON artifacts returned no matches. Final independent review verdict: READY with no Critical or Important findings.
